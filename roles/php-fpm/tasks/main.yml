---
- name: Install PHP-FPM
  apt:
    name: "php{{ php_version }}-fpm"
    state: present
    update_cache: yes
  tags: php-fpm

- name: Start php-fpm
  service:
    name: "php{{ php_version }}-fpm"
    state: started
    enabled: yes
  tags: php-fpm